{% extends 'base.html.twig' %}

{% block title %}Article index{% endblock %}

{% block body %}
    <h1>Blog</h1>

    <div class="container">
    {% for article in articles %}
        <div class="content">
            <h1>{{ article.title }}</h1>
            <p class="publish">{{ article.publishDate ? article.publishDate|date('D-m-y') : '' }}</p>
            <div class="article__content">
                <div class="article__image">
                    <img src='../img/zut.png'>
                </div>
                <div class="article__text">
                    <p class="teaser">{{ article.teaser }}</p>
                    <p class="body">{{ article.body | raw }}</p>
                </div>
            </div>
        </div>
        {% endfor %}
    </div>

    {# <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>Body</th>
                <th>Picture</th>
                <th>PublishDate</th>
                <th>Teaser</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for article in articles %}
            <tr>
                <td>{{ article.id }}</td>
                <td>{{ article.title }}</td>
                <td>{{ article.body }}</td>
                <td>{{ article.picture }}</td>
                <td>{{ article.publishDate ? article.publishDate|date('Y-m-d') : '' }}</td>
                <td>{{ article.teaser }}</td>
                <td>
                    <a href="{{ path('article_show', {'id': article.id}) }}">show</a>
                    <a href="{{ path('article_edit', {'id': article.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('article_new') }}">Create new</a> #}
{% endblock %}
